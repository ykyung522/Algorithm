#include <stdio.h>

int main(void) {
    int a;
    int b;
    scanf("%d %d", &a, &b);
    printf("a = %d\nb = %d\n", a, b);
    return 0;
    //n;main(i){for(;~i;printf("%c = %d\n",98-i--,n))scanf("%d",&n);}
    //다른 형태의 풀이 과정
    //비트 NOT 연산자를 사용하여 98-i로 아스키 코드 값으로 변환됨. a부터 j까지 출력을 생성
    
}
